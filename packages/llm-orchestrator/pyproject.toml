[project]
name = "mega-orchestrator"
version = "0.1.0"
description = "Orquestrador multi-LLM (stub)"
authors = [ { name = "MEGA" } ]
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where=["src"]