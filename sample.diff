--- a/component.js
+++ b/component.js
@@ -5,5 +5,6 @@
   const [count, setCount] = useState(0);
 
   const increment = () => {
-    setCount(count + 1);
+    // Incrementa o contador e também loga no console
+    setCount(count + 1);
+    console.log("Contador incrementado!");
   };
 
   return (
